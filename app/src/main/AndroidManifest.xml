<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cst2335.finalproject">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-feature android:name="android.hardware.type.watch" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProject"
        android:usesCleartextTraffic="true">
        <activity android:name=".SongsterrEmptyActivity" android:theme="@style/Theme.Songsterr"></activity>
        <activity android:name=".SongsterrFavourites" android:theme="@style/Theme.Songsterr" />
        <activity android:name=".SongsterrSearchActivity" android:theme="@style/Theme.Songsterr" />
        <activity android:name=".SoccerSignInActivity"></activity>
        <activity android:name=".SoccerMainActivity" />
        android:theme="@style/Theme.FinalProject">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".CarTraderFavoritesActivity" />
        <activity android:name=".CarTraderSelectionActivity" />
        <activity android:name=".CarTraderMainActivity" />
        <activity android:name=".SongsterrMainActivity" android:theme="@style/Theme.Songsterr" />
        <activity android:name=".CarTraderModelDetailsActivity" />
        <activity android:name=".TriviaMainActivity" />
        <activity android:name=".TriviaGameplayActivity" />
        <activity android:name=".TriviaScoreActivity" />
        <activity android:name=".TriviaFinalActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>