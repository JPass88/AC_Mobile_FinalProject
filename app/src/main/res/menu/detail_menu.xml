<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/fav_item"
        android:title="@string/AT_AddFavorites"
        android:icon="@drawable/google_icon"
        app:showAsAction="always"
        android:orderInCategory="200"/>

    <item android:id="@+id/at_item"
        android:title="@string/AT_CheckAT"
        android:icon="@drawable/google_icon"
        app:showAsAction="always"
        android:orderInCategory="300"/>

    <item android:id="@+id/google_item"
        android:title="@string/AT_CheckGoogle"
        android:icon="@drawable/google_icon"
        app:showAsAction="always"
        android:orderInCategory="400"/>

</menu>